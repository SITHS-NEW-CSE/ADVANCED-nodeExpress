// you can run JS files on your terminal directly (without a browser)
// Node.JS is a JS engine installed on your computer which executes JS code 
// browsers have different types of JS engines too (Chrome: V8, Firefox: SpiderMonkey)

// run this file using `node demo.js`
const x = 10;
const y = 20;
console.log(x + y);


// NOTE: the window and document objects are nonexistent on Node.js 
// because you won't be running this code on the browser

// console.log(window); // will result in an error
// console.log(document);